{
  "buildCommand": "pip install -r src/server/python/requirements.txt && pip install git+https://github.com/microsoft/Markitdown.git && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs"
}
